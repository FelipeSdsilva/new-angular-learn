specVersion: "v1beta"
serviceId: "cloudStorage"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "cloudStorage-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
